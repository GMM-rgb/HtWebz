## HtWebz Development
- Create the new account system for the backend & frontend resolver.
- I need to work on building the Onboarding System; for when a new browser session is created.<br/>
This will result in Signing up / Logging in after this is complete. Otherwise, skips all of that and goes to the homepage, which overall; is if they are still logged in the site.
