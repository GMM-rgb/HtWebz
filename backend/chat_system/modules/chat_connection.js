const path = require("path");
const fs = require("fs");

class ChatConnectionManager {
    /**
     * 
     * @returns {void}
     */
    static ConnectToNewConversation() {
        
    }
}

module.exports = {
    ChatConnectionManager
}
